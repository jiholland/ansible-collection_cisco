---
# tasks file for backup

- name: backup_cisco.ios.ios | Backup running-config to local directory.
  cisco.ios.ios_config:
    backup: true
    backup_options:
      filename: "{{ backup_filename }}"
      dir_path: "{{ backup_temp_dir_output['path'] }}/{{ backup_time }}"
  notify: Push to git.
